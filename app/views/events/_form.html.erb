<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, autocomplete: "off" %>
  </div>

  <div class="field">
    <%= f.label :max_attendance %><br />
    <%= f.text_field :max_attendance, autocomplete: "off" %>
  </div>

  <div class="field">
    <%= f.label :start_time %><br />
    <%= f.datetime_select :start_time, autocomplete: "off" %>
  </div>

  <div class="field">
    <%= f.label :create_time %><br />
    <%= f.datetime_select :create_time, autocomplete: "off" %>
  </div>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, autocomplete: "off" %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
