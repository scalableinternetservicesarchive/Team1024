<%= form_for(@fake_user) do |f| %>
  <% if @fake_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fake_user.errors.count, "error") %> prohibited this fake_user from being saved:</h2>

      <ul>
      <% @fake_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
