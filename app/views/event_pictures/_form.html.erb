<%= form_for(@event_picture) do |f| %>
  <% if @event_picture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_picture.errors.count, "error") %> prohibited this event_picture from being saved:</h2>

      <ul>
      <% @event_picture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event_id %><br>
    <%= f.number_field :event_id %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
